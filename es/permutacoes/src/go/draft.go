package main

import "fmt"

func backtrack(input []rune) {
	for i := 0; i < len(input); i++ {

		input[i]
	}
}

func permute(input []rune, start int) {

}

func main() {
	str := ""
	fmt.Scan(&str)
	input := []rune(str)
	permute(input, 0)
}
